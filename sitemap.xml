---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<!-- XML 선언 -->

<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- /jinhwi 사이트의 모든 URL을 나열 -->

  <url>
    <!-- /jinhwi 홈(섹션 루트) -->
    <loc>{{ site.url }}{{ site.baseurl }}/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
  </url>

  {% comment %}
    /jinhwi repo에 포스트가 있다면 포함
  {% endcomment %}
  {% for post in site.posts %}
  <url>
    <!-- /jinhwi 포스트 URL -->
    <loc>{{ site.url }}{{ site.baseurl }}{{ post.url | remove: 'index.html' }}</loc>
    <lastmod>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</lastmod>
  </url>
  {% endfor %}

  {% comment %}
    /jinhwi repo 내 일반 페이지들 포함 (레이아웃 있는 페이지만, feed 제외)
  {% endcomment %}
  {% for page in site.pages %}
    {% if page.layout and page.layout != 'feed' %}
      {% assign lm = page.last_modified_at | default: page.date | default: site.time %}
  <url>
    <!-- /jinhwi 페이지 URL -->
    <loc>{{ site.url }}{{ site.baseurl }}{{ page.url | remove: 'index.html' }}</loc>
    <lastmod>{{ lm | date_to_xmlschema }}</lastmod>
  </url>
    {% endif %}
  {% endfor %}

  {% comment %}
    (선택) /jinhwi 안에서도 태그/카테고리 인덱스가 있다면 포함
    없다면 아래 두 블록은 삭제해도 됩니다.
  {% endcomment %}
  <url>
    <!-- /jinhwi 태그 인덱스 (있을 경우) -->
    <loc>{{ site.url }}{{ site.baseurl }}/tags/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
  </url>

  <url>
    <!-- /jinhwi 카테고리 인덱스 (있을 경우) -->
    <loc>{{ site.url }}{{ site.baseurl }}/categories/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
  </url>

</urlset>
<!-- urlset 끝 -->